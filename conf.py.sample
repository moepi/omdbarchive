import logging,sys
logging.basicConfig(stream=sys.stdout, level=logging.INFO)
log=logging.getLogger()

# path of stylesheet sourced by html files
stylesheet="style.css"
title="My very own movie database"
sorted=True
# list of pathes
directories=["/path/to/folder1","/path/to/folder2"]
# directory the html files will end up in (default: directory the script is executed from)
outputdir="."
